spring:
  application:
    name: loan-origination-system
  datasource:
    url: jdbc:mysql://localhost:3306/loan_origination_system
    username: root
    password: Sasi@1457
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080